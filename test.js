// require('dotenv').config()
// const { Client } = require('@notionhq/client');

// const notion = new Client({ auth: process.env.NOTION_API_KEY });

// (async () => {
//   const response = await notion.users.list();
//   console.log(response);
// })();

for (const x of [123, 44, 414]) {
  console.log(x);
}
